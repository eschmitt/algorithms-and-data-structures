//+ getValuesInRange :: [a] -> [a] -> [a]
var getValuesInRange = function (list, range) {
      var start = 0
        , stop = list.length - 1
        ;
      while (stop >= start) {
        if ()
      }
      return null;
    }
  ;

module.exports = {
  getValuesInRange: getValuesInRange
}
